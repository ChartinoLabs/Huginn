test_cases:
  "1.0.0":
    title: Verify BGP session state
    job: jobs/verify_bgp.py

test_case_groups:
  state-baseline:
    name: State Baseline Checks
    tests:
      - "1.0.0"

scenarios:
  scenario-1:
    name: Pre-Change Validation
    phases:
      steady-state:
        name: Steady State Verification
        test_case_groups:
          - state-baseline
